// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("scopext", "for scopext support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("scopext", "enable scopext support", "no");

if (PHP_SCOPEXT != "no") {
	EXTENSION("scopext", "scopext.c");
}

